#!/usr/bin/env bash

DOTFILES="$HOME/i3-dotfiles-fedora"
OUTFILE="$DOTFILES/pkg.list"

mkdir -p "$DOTFILES"

echo "Exporting installed packages..."
dnf repoquery --userinstalled --queryformat '%{name}\n' 2>/dev/null | sort > "$OUTFILE"

echo "Done."
echo
echo "Saved: $OUTFILE"
